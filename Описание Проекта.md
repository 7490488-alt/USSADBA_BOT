# 🤖 USSDABA_BOT - Телеграм-бот для проекта "Усадьба"

Телеграм-бот для автоматизации общения с клиентами, инвесторами и партнерами проекта "Усадьба". Бот предоставляет документы, отвечает на вопросы через Yandex GPT и управляет диалогами с пользователями.

## 📋 Оглавление

- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Установка и запуск](#-установка-и-запуск)
- [Конфигурация](#-конфигурация)
- [Структура проекта](#-структура-проекта)
- [API и интеграции](#-api-и-интеграции)
- [Развертывание](#-развертывание)
- [Возможные проблемы и решения](#-возможные-проблемы-и-решения)
- [Разработка и расширение](#-разработка-и-расширение)
- [Лицензия](#-лицензия)

## 🌟 Возможности

- **📄 Управление документами**: Автоматическая отправка бизнес-планов, презентаций и финансовых моделей
- **💬 Интеллектуальный чат**: Интеграция с Yandex GPT для ответов на вопросы пользователей
- **🎛 Контекстные сценарии**: Разные режимы ответов для покупателей, инвесторов и партнеров
- **📊 История диалогов**: Сохранение и загрузка истории conversations
- **⚡ Кэширование**: Оптимизация производительности через кэширование ответов
- **📝 Логирование**: Детальное логирование всех операций бота

## 🛠 Технологии

- **Python 3.12** - основной язык программирования
- **python-telegram-bot 20.x** - работа с Telegram API
- **Pydantic** - валидация конфигурации и настроек
- **Yandex Cloud API** - интеграция с Yandex GPT
- **aiofiles** - асинхронная работа с файлами
- **httpx** - асинхронные HTTP-запросы

## 📦 Установка и запуск

### Предварительные требования

- Python 3.10 или выше
- Аккаунт в Telegram с созданным ботом через @BotFather
- Аккаунт в Yandex Cloud с доступом к Yandex GPT API

### 1. Клонирование репозитория

```bash
git clone https://github.com/7490488-alt/USSADBA_BOT
cd ussadba_bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Создайте файл `.env` в корневой директории:

```env
BOT_TOKEN=your_telegram_bot_token_here
YA_API_KEY=your_yandex_cloud_api_key_here
YA_FOLDER_ID=your_yandex_cloud_folder_id_here
ADMIN_CHAT_ID=your_admin_chat_id_here
DEBUG=false
```

### 4. Подготовка директорий и файлов

Создайте необходимые директории и файлы:

```bash
# Создание директорий (автоматически при первом запуске)
mkdir -p data/documents data/history data/logs prompts

# Добавление документов (пример)
cp /path/to/your/documents/* data/documents/

# Создание промптов
echo "Ты - ассистент проекта «Усадьба». Отвечай вежливо и профессионально на вопросы о проекте." > prompts/general.txt
echo "Ты - консультант по продажам проекта «Усадьба». Помогай потенциальным покупателям." > prompts/buyer.txt
# ... аналогично для других промптов
```

### 5. Запуск бота

```bash
python main.py
```

## ⚙️ Конфигурация

### Основные настройки (config.py)

- `bot_token`: Токен Telegram-бота
- `ya_api_key`: API-ключ Yandex Cloud
- `ya_folder_id`: ID каталога Yandex Cloud
- `documents_dir`: Путь к директории с документами
- `history_dir`: Путь к директории с историей диалогов
- `logs_dir`: Путь к директории с логами
- `prompts_dir`: Путь к директории с промптами

### Настройки LLM

- `llm_temperature`: Температура для генерации ответов (0.0-1.0)
- `llm_max_tokens`: Максимальное количество токенов в ответе
- `max_history_pairs`: Максимальное количество пар вопрос-ответ в истории
- `rate_limit_sec`: Ограничение частоты запросов

## 📁 Структура проекта

```
ussadba_bot/
│
├── main.py                     # Точка входа: запуск и управление жизненным циклом
├── config.py                   # Настройки через Pydantic
├── requirements.txt            # Зависимости Python
├── .env                        # Переменные окружения (не в репозитории)
│
├── data/                       # Данные бота
│   ├── documents/              # PDF, XLSX, DOCX - документы для отправки
│   ├── history/                # JSON-файлы с историей диалогов
│   └── logs/                   # Логи по датам
│
├── prompts/                    # Промпты для разных интентов
│   ├── general.txt             # Общий промпт
│   ├── buyer.txt               # Промпт для покупателей
│   ├── investor.txt            # Промпт для инвесторов
│   ├── partner.txt             # Промпт для партнеров
│   └── prompt_map.json         # Маппинг ключевых слов к интентам
│
├── bot/                        # Логика бота
│   ├── __init__.py             # Пакет Python
│   ├── application.py          # Регистрация обработчиков
│   └── handlers/               # Обработчики команд
│       ├── start.py            # Приветствие и кнопки
│       ├── docs.py             # Отправка документов
│       ├── chat.py             # Диалог с ИИ
│       └── other.py            # Обработка других типов сообщений
│
├── services/                   # Сервисы
│   ├── llm.py                  # Клиент Yandex LLM
│   ├── cache.py                # Кэширование ответов ИИ
│   ├── history.py              # Загрузка/сохранение истории
│   └── intent_recognizer.py    # Определение интента пользователя
│
└── utils/                      # Вспомогательные утилиты
    ├── logger.py               # Настройка логирования
    └── helpers.py              # Вспомогательные функции
```

## 🔌 API и интеграции

### Telegram Bot API

Бот использует официальный Telegram Bot API через библиотеку `python-telegram-bot` для:
- Получения и отправки сообщений
- Управления клавиатурами и интерфейсом
- Обработки команд и callback-запросов

### Yandex Cloud API

Интеграция с Yandex GPT через Yandex Cloud API:
- Аутентификация по API-ключу
- Отправка промптов и получение ответов
- Управление параметрами генерации (temperature, maxTokens)

## 🚀 Развертывание

### Локальное развертывание

1. Установите зависимости: `pip install -r requirements.txt`
2. Настройте переменные окружения в `.env`
3. Запустите бота: `python main.py`

### Развертывание на сервере

Рекомендуется использовать процесс менеджеры для постоянной работы:

#### С помощью systemd

Создайте файл `/etc/systemd/system/ussadba-bot.service`:

```ini
[Unit]
Description=USSADBA Telegram Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/path/to/ussadba_bot
Environment=PYTHONPATH=/path/to/ussadba_bot
ExecStart=/usr/bin/python3 /path/to/ussadba_bot/main.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

Затем выполните:
```bash
sudo systemctl daemon-reload
sudo systemctl enable ussadba-bot
sudo systemctl start ussadba-bot
```

#### С помощью Docker

Создайте `Dockerfile`:

```dockerfile
FROM python:3.12-slim

WORKDIR /app
COPY . .
RUN pip install -r requirements.txt

CMD ["python", "main.py"]
```

Соберите и запустите контейнер:
```bash
docker build -t ussadba-bot .
docker run -d --name ussadba-bot --env-file .env ussadba-bot
```

## 🐛 Возможные проблемы и решения

### 1. Бот не отвечает на сообщения

**Решение:** 
- Проверьте токен бота в `.env`
- Убедитесь, что бот не заблокирован в Telegram
- Проверьте логи на наличие ошибок

### 2. Ошибки при отправке документов

**Решение:**
- Убедитесь, что файлы существуют и не пустые
- Проверьте права доступа к файлам
- Убедитесь, что расширения файлов входят в `allowed_document_types`

### 3. Ошибки доступа к Yandex API

**Решение:**
- Проверьте правильность API-ключа и ID каталога
- Убедитесь, что сервисный аккаунт имеет нужные права
- Проверьте квоты и лимиты в Yandex Cloud

### 4. Большая задержка ответов

**Решение:**
- Включите кэширование в настройках
- Оптимизируйте промпты для более коротких ответов
- Рассмотрите возможность увеличения лимитов Yandex API

## 🔧 Разработка и расширение

### Добавление новых команд

1. Создайте обработчик в `bot/handlers/`
2. Зарегистрируйте обработчик в `bot/application.py`
3. Добавьте описание команды в `start.py`

### Добавление новых типов документов

1. Добавьте расширение в `config.py` → `allowed_document_types`
2. Положите файлы в `data/documents/`
3. Обновите логику в `docs.py` при необходимости

### Кастомизация промптов

Отредактируйте файлы в папке `prompts/`:
- `general.txt` - общие ответы о проекте
- `buyer.txt` - ответы для потенциальных покупателей
- `investor.txt` - ответы для инвесторов
- `partner.txt` - ответы для партнеров

### Мониторинг и логирование

Логи сохраняются в `data/logs/` с ежедневной ротацией. Для мониторинга используйте:

```bash
# Просмотр логов в реальном времени
tail -f data/logs/bot_$(date +%Y-%m-%d).log

# Поиск ошибок
grep -r "ERROR" data/logs/
```

## 📄 Лицензия

Этот проект является собственностью проекта "Усадьба". Все права защищены.

## 🤝 Поддержка

По вопросам работы бота обращайтесь:
- К разработчикам через Issues репозитория
- В Telegram: @your_support_contact

---

**Примечание:** Перед развертыванием в продакшене обязательно:
1. Протестируйте все функции бота
2. Настройте мониторинг и оповещения
3. Обеспечьте регулярное резервное копирование данных
4. Установите правильные права доступа к файлам